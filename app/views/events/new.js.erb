// use the empty div remote_container and then render the 'new' partial which has a new event
$('#remote_container').html('<%= j render "new" %>');
// and call the modal show on it
$('#new_event').modal('show');