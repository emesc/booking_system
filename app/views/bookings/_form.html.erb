<%= simple_form_for @booking do |f| %>
  <%= f.association :program, label_method: :program_title %>
  <%= f.input :preferred_date, as: :date, label: "Select your preferred date" %>
  <%= f.button :submit %>
<% end %>